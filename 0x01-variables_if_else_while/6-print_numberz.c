#include <stdio.h>
/**
 * main - prints all single digit numbers of base 10 starting from 0
 * Return: 0(success)
 */

int main(void)
{
	int a;
	
	for (a =22; a < 32; a++)
	{
		putchar(a);
	}

	putchar('\n');
	return (0);
}
