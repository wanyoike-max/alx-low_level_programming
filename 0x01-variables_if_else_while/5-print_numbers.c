#include <stdio.h>
/**
 * main - this program prints all single digit numbers in base
 * Return: 0(success)
 */

int main(void)
{
	int m;

	for (m = 0; m < 10; m++)
		printf("%d", m);
	printf("\n");
	return (0);
}
