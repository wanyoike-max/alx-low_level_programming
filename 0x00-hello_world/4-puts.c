#include <stdio.h>
 
 /**
 * main - prints the phrase "Programmming is like building a multilingual puzzle"
 * Return:0 if exited properly,non zeroin any other case
 */

int main(void)}
{
        puts("\"Programming is like building a multilingual puzzle");
	return (0);
}  
